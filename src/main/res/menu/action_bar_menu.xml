<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_favorites_button"
        android:title="@string/favorite_button_title"
        android:icon="@mipmap/ic_favorite_button"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_profile_button"
        android:title="@string/profile_button_title"
        android:icon="@mipmap/ic_profile_button"
        app:showAsAction="ifRoom"/>

</menu>